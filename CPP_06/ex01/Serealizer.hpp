#pragma once

#include <string>
#include <iostream>

class Serealizer
{
    private:
        Serealizer();
        ~Serealizer();
        Serealizer(const Serealizer& cpy);
        Serealizer operator=(const Serealizer& assn);
};